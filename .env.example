# Database (SQLite for local/demo; swap provider to "postgresql" + a Neon/Supabase URL for deployment)
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_SECRET="replace-with-a-random-32-byte-string"
NEXTAUTH_URL="http://localhost:3000"

# OpenAI (used for AI Transport Assistant, Document Checker, Service Recommendation)
OPENAI_API_KEY="sk-...your-key-here..."
